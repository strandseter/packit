<Page
    x:Class="Packit.App.Views.DetailTripV2Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:model="using:Packit.Model"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
    xmlns:datalinks="using:Packit.App.DataLinks"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">

    <i:Interaction.Behaviors>
        <ic:EventTriggerBehavior EventName="Loaded">
            <ic:InvokeCommandAction Command="{x:Bind ViewModel.LoadedCommand}" />
        </ic:EventTriggerBehavior>
    </i:Interaction.Behaviors>

    <Page.Resources>
        <DataTemplate x:Key="ItemGridTemplate" x:DataType="model:Item">
            <Grid>
                <StackPanel Orientation="Vertical" Height="100" VerticalAlignment="Bottom" Padding="5,1,5,1" 
                    Background="LightGray" Opacity=".80">
                    <TextBlock Text="{x:Bind Title}" FontSize="20" Foreground="Black"/>
                    <StackPanel Orientation="Horizontal" >
                        <TextBlock FontSize="15" Text="{x:Bind Description}" Style="{ThemeResource CaptionTextBlockStyle}" 
                           Foreground="{ThemeResource SystemBaseMediumHighColor}"/>
                    </StackPanel>
                    <CheckBox/>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid x:Name="ContentArea" Margin="{StaticResource MediumLeftRightMargin}">

        <Grid.Background>
            <ImageBrush Stretch="UniformToFill" ImageSource="{x:Bind ViewModel.TripWithImageWeather.Image, Mode=OneWay}" />
        </Grid.Background>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">

                <VisualState x:Name="VisualStateNarrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ContentArea.Margin" Value="12"/>
                        <Setter Target="FirstGrid.Margin" Value="0 0 0 6"/>
                        <Setter Target="SecondGrid.Margin" Value="0 6 0 0"/>
                        <Setter Target="ContentArea.RowDefinitions[1].Height" Value="*"/>
                        <Setter Target="ContentArea.ColumnDefinitions[1].Width" Value="auto"/>
                        <Setter Target="SecondGrid.(Grid.Column)" Value="0"/>
                        <Setter Target="SecondGrid.(Grid.Row)" Value="1"/>
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="VisualStateNormal">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="800" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ContentArea.Margin" Value="24"/>
                        <Setter Target="ContentArea.RowDefinitions[1].Height" Value="auto"/>
                        <Setter Target="ContentArea.ColumnDefinitions[1].Width" Value="*"/>
                        <Setter Target="FirstGrid.Margin" Value="0 0 6 0"/>
                        <Setter Target="SecondGrid.Margin" Value="6 0 0 0"/>
                        <Setter Target="SecondGrid.(Grid.Column)" Value="1"/>
                        <Setter Target="SecondGrid.(Grid.Row)" Value="0"/>
                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid x:Name="FirstGrid" Grid.Column="0" Grid.Row="0">
            <StackPanel Grid.Row="1">

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                    <TextBlock VerticalAlignment="Top"
                        Text="{x:Bind ViewModel.TripWithImageWeather.Trip.Title, Mode=OneWay}"
                        Style="{StaticResource HeaderTextBlockStyle}"
                           x:Name="TextTest"/>

                    <Ellipse Height="60" Width="60" VerticalAlignment="Top">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="{x:Bind ViewModel.TripWithImageWeather.Image, Mode=OneWay}" Stretch="UniformToFill"/>
                        </Ellipse.Fill>
                    </Ellipse>

                </StackPanel>

                <TextBlock
                       Text="Packing list"
                       HorizontalAlignment="Center"
                       Style="{StaticResource SubheaderTextBlockStyle}"/>

                <ListView ItemsSource="{x:Bind ViewModel.Backpacks}" HorizontalAlignment="Stretch" Background="White" Opacity="0.9" Margin="10">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="datalinks:BackpackWithItems">
                            <StackPanel Orientation="Vertical">

                                <StackPanel Orientation="Horizontal">

                                    <TextBlock Text="{x:Bind Backpack.Title}" Style="{StaticResource TitleTextBlockStyle}"/>

                                    <Button Content="...">
                                        <Button.Flyout>
                                            <MenuFlyout>
                                                <MenuFlyoutItem Text="Share">
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE72D;"/>
                                                    </MenuFlyoutItem.Icon>
                                                </MenuFlyoutItem>
                                                
                                                <MenuFlyoutItem Text="Copy" Icon="Copy"/>

                                                <MenuFlyoutItem Text="Delete" Icon="Delete"/>

                                                <MenuFlyoutSeparator/>
                                                <MenuFlyoutItem Text="Rename"/>
                                                <MenuFlyoutItem Text="Select"/>
                                            </MenuFlyout>
                                        </Button.Flyout>
                                    </Button>
                                </StackPanel>

                                <controls:AdaptiveGridView ItemsSource="{x:Bind Items}"
                                                           ItemTemplate="{StaticResource ItemGridTemplate}"
>

                                    <GridView.ItemContainerStyle>
                                        <Style TargetType="GridViewItem">
                                            <Setter Property="Margin" Value="2, 2, 2, 2"/>
                                        </Style>
                                    </GridView.ItemContainerStyle>

                                    <GridView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <ItemsWrapGrid x:Name="MaxItemsWrapGrid" Orientation="Horizontal" MaximumRowsOrColumns="5"/>
                                        </ItemsPanelTemplate>
                                    </GridView.ItemsPanel>

                                </controls:AdaptiveGridView>

                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

        </Grid>

        <Grid x:Name="SecondGrid" Grid.Column="1" Grid.Row="0">
            <StackPanel Margin="10" HorizontalAlignment="Center">
                <TextBlock Text="{x:Bind ViewModel.TripWithImageWeather.WeatherReport.City, Mode=OneWay}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
